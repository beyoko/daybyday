---
import type { MarkdownLayoutProps } from 'astro';

type Props = MarkdownLayoutProps <{
  className?: string
}>

const { className = '' } = Astro.props

---

<article class={`px-8 mx-auto max-w-3xl ${className}`}>
  <slot />
</article>
