---
import type { MarkdownHeading } from 'astro'
import Link from '@/components/Link.astro'

type Props = {
  headings: MarkdownHeading[]
}

const { headings } = Astro.props
---

<div>
  <div
    class="overflow-y-auto overscroll-contain rounded-md p-3 border-gray-800 dark:border-gray-200 border-2"
  >
    <ol class="list-none pl-0 my-0 ml-1 relative">
      {
        headings.map((heading) => (
          <li class="relative pl-2">
            <Link
              className="block text-gray-600 hover:text-gray-800 no-underline font-normal dark:text-gray-200 dark:hover:text-gray-100"
              href={`#${heading.slug}`}
            >
              {heading.text}
            </Link>
          </li>
        ))
      }
    </ol>
  </div>
</div>
